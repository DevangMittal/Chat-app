# MongoDB Atlas Connection String
# Replace with your actual MongoDB Atlas credentials
MONGO_URL=mongodb+srv://<username>:<password>@<cluster-url>/chat-app?retryWrites=true&w=majority&appName=ChatAppCluster

# Server Configuration
PORT=5000

# JWT Secret (for future authentication enhancements)
JWT_SECRET=your-super-secret-jwt-key-here

# CORS Configuration (for production)
FRONTEND_URL=http://localhost:3000
